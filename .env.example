# Optional. Use a Figma token with the narrowest available file read scope.
FIGMA_ACCESS_TOKEN=

# Optional. Enables --cleanup openai; deterministic rendering works without it.
OPENAI_API_KEY=

# Optional. Leave blank to use the package default cleanup model.
OPENAI_MODEL=
